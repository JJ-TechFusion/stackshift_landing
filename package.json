{
  "name": "stackshift-landing",
  "version": "1.0.0",
  "description": "Landing page for Stackshift.cloud",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && npx tailwindcss -i ./input.css -o ./dist/styles.css --minify && cp index.html blog.html script.js favicon.svg dist/",
    "watch": "npx tailwindcss -i ./input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
